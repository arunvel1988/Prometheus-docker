groups:
- name: nginx-alerts
  rules:
  - alert: NginxDown
    expr: up{job="nginx_exporter"} == 0
    for: 1m
    labels:
      severity: critical
    annotations:
      summary: "NGINX EXPORTER is down"
      description: "NGINX has been down for more than 1 minutes"
